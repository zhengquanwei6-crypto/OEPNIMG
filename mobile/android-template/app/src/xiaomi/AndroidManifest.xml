<?xml version="1.0" encoding="utf-8"?>
<!--
  小米渠道专属 Manifest 片段（merge 到 main 的 AndroidManifest）。

  ⚠️ 接入正式 SDK 后，请按 MiPush 文档替换 TODO 标记的组件。
  AppId / AppKey 已通过 BuildConfig 注入（android-template/app/build.gradle 中定义）。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.oepnimg.app">

    <!-- 小米推送专属权限（${applicationId} 占位符会在合并时被实际包名替换） -->
    <permission
        android:name="${applicationId}.permission.MIPUSH_RECEIVE"
        android:protectionLevel="signature" />
    <uses-permission android:name="${applicationId}.permission.MIPUSH_RECEIVE" />

    <application tools:targetApi="34">

        <!-- TODO MiPush: 接入 SDK 后取消注释并填入 SDK 提供的类名
        <service
            android:name="com.xiaomi.push.service.XMPushService"
            android:enabled="true"
            android:process=":pushservice" />

        <service
            android:name="com.xiaomi.push.service.XMJobService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:process=":pushservice" />

        <receiver
            android:name=".push.MiPushMessageReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xiaomi.mipush.RECEIVE_MESSAGE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.MESSAGE_ARRIVED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.ERROR" />
            </intent-filter>
        </receiver>
        -->

        <!-- 默认 Push channel（MIUI 必须显式声明 channel） -->
        <meta-data
            android:name="oepnimg.push.default_channel_id"
            android:value="oepnimg_default" />

    </application>
</manifest>
